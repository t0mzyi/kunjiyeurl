<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>kunjyeurl</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        
        body {
            min-height: 100vh;
            background-color: #f8f9fa;
            display: flex;
            align-items: center; 
            justify-content: center;
        }

        .main-card {
            /* Max width for a clean look on large screens, centered */
            max-width: 550px;
            width: 90%;
            /* Minimal shadow for depth */
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
        }

        .result-box {
            /* Highlight the result with a success background */
            background-color: #e6f7ed; /* Very light success color */
            border: 1px solid #c3e6cb;
            padding: 1rem;
            border-radius: 0.25rem;
            margin-top: 1.5rem;
        }

        .result-link {
            font-size: 1.15rem;
            font-weight: 500;
            color: #155724; /* Dark success text color */
            word-break: break-all; /* Ensures long URLs wrap correctly */
        }

        .btn-primary {
            /* Customizing the primary button color slightly for a fresher look */
            background-color: #00bcd4; /* Fresh Cyan */
            border-color: #00bcd4;
        }

        .btn-primary:hover {
            background-color: #0097a7;
            border-color: #0097a7;
        }

        .form-control:focus {
             /* Custom focus color for input to match button */
            border-color: #00bcd4;
            box-shadow: 0 0 0 0.25rem rgba(0, 188, 212, 0.25);
        }
    </style>
</head>
<body>

    <div class="main-card card p-4">
        <div class="card-body">
            <h1 class="card-title text-center mb-4 text-primary fw-bold">
                <span class="fs-4 me-2">🔗</span> Cheriya Link
            </h1>
            <p class="card-subtitle mb-4 text-muted text-center">
                Paste your long URL below to get a shorter, cleaner link.
            </p>
            
            <form action="/shorten" method="post" class="d-flex gap-2">
                <input 
                    type="text" 
                    name="longUrl" 
                    class="form-control form-control-lg" 
                    placeholder="e.g., https://very.long.website.com/path/to/content" 
                    required
                >
                <button type="submit" class="btn btn-primary btn-lg flex-shrink-0">
                    Shorten
                </button>
            </form>

            <% if (typeof shortUrl !== 'undefined') { %>
                <div class="result-box text-center">
                    <p class="mb-2 fw-semibold text-success">
                        ✅ Your short link is ready!
                    </p>
                    <a href="<%= shortUrl %>" class="result-link">
                        <%= shortUrl %>
                    </a>
                </div>
            <% } %>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>